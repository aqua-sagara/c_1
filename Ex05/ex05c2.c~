#include <stdio.h>
main()
{
  int score,count,pass=0,notpass,sum=0,all=0;
  double ave;
  while(score>0){
  printf("得点を入力してください\n");
  scanf("%d",&score);
all+=1
  if(score>=50)
    pass+=1;
  if(score<50)
    notpass+=1;
  }
  sum=sum+score;
  ave=sum/all;
  printf("受験者は%d人で、平均点は%f点です\n",all,ave);
  printf("合格者は３名、不合格者は１名です\n");




