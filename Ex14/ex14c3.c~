#include <stdio.h>
int isprime(int);
main()
{
  int a[10][10],i,j=0,b;
  for(i=2;i<=100;i++){
    if( 1==isprime(i) ){
      printf("%d ", i);
      j++;
      if(j==9){
	printf("\n");
	j=0;
      }
    }
  }
  return;

    for(j=0;j<10;j++){
      printf("%d")
	}
    printf("\n")
      }
}

int isprime(int i){
    int i;
 
    if(n < 2)
        return 0;
    else if(n == 2)
        return 1;
 
    if(n % 2 == 0)
        return 0;
 
    for(i = 3; i <= n / i; i += 2)
         if(n % i == 0)
            return 0;
    return 1;
}
