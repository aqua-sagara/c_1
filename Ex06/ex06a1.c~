#include <studio.h>
main()
{
  int n,sum=0;
  int i;
  printf("整数を入力して下さい");
  scanf("%d",&n);
  for(初期化；条件式;カウンター変数の更新）{
    if(i%5)==0){
    sum +=i;
  }
}
printf("%d以下の５の倍数となる正の整数の和は%dです\n",n,sum);
}
