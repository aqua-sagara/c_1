#include <stdio.h>
main()
{
  int n,i,a;
  printf("1から9までの整数を入力してください:");
  scanf("%d",&n);
  if(n>10)
    printf("入力する數は1から9までです\n");
  else if(n>0&&n<10)
    for(i=1;i<=n;i++)
      {
	for(a=1;a<=i;a++)
	  printf("%d",n);
      }
}


